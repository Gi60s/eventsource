var request             = require('request');

module.exports = client;

function client(serverUrl) {
    "use strict";
    var factory = {};

    factory.command = function() {

    };

    factory.query = function() {

    };

    factory.subscribe = function(filter) {

    };

    return factory;
}